<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :Name %><br>
    <%= f.text_field :commenter%>
  </p>
  <p>
    <%= f.label :Content %><br>
    <%= f.text_area :body, :size => '40x10' %>
  </p>
  <p>
    <%= f.submit "发表评论" %>
  </p>
<% end %>