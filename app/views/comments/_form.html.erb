<%= form_for([@article, @article.comments.build]) do |f| %>
  
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@comments.errors.count, "error") %> prohibited
        this comment from being saved:
      </h2>
      <ul>
        <% @comments.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :评论人 %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :内容 %><br>
    <%= f.text_area :body, :size => '40x10' %>
  </p>
  <p>
    <%= f.submit "发表评论" %>
  </p>
<% end %>